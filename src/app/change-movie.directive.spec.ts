import { ChangeMovieDirective } from './change-movie.directive';

describe('ChangeMovieDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeMovieDirective();
    expect(directive).toBeTruthy();
  });
});
