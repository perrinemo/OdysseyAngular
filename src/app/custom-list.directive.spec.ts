import { CustomListDirective } from './custom-list.directive';

describe('CustomListDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomListDirective();
    expect(directive).toBeTruthy();
  });
});
